07/16
pip install = python -m pip.py → 파이썬이 이 파일을 모듈로써 실행하게 해라
들여쓰기는 문법이고 안해주면 systax error 발생
함수, 클래스 등을 만들때 내용이 없으면 오류가 나지만 pass 사용시 에러가 발생하지 않음.
연산자는 좌항 우항 2개가 필요한 2이항연산 타입확인

반복문 for
조건문 while / if

카운트블객체 이터러블객체를 넣으면 사람이 카운트하는 숫자로 리턴